import React from 'react';
import styles from './ErrorOccurred.module.css';

const ErrorOccurred = () => (
  <div className={styles.error}>An unknown error has occurred</div>
);

ErrorOccurred.propTypes = {};

export default ErrorOccurred;
